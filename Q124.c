#include <stdio.h>
int main ()
{
    int i,p,n,result;
    scanf("%d",&a,&b);
  
     for (i = 0; i <= p; i++) {
        if (i == 0) continue;
        result *= n;

     }
     printf("%d",result);
    return 0;
}